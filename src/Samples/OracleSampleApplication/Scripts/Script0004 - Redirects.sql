-- Settings and Statistics

create table REDIRECT(
    ID INTEGER GENERATED ALWAYS AS IDENTITY (start with 1 increment by 1 nocycle),
    REDIRECT_FROM VARCHAR2(255) NOT NULL,
    REDIRECT_TO VARCHAR2(255) NOT NULL,
    CONSTRAINT PK_REDIRECT_ID PRIMARY KEY (ID) ENABLE VALIDATE
)

